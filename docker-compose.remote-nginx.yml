version: '3.8'

services:
  nginx-proxy:
    image: nginx:alpine
    container_name: aligndesigns-nginx-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx/remote-nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
